server {
    listen 80;
    server_name localhost;
    
    location / {
        root /usr/share/nginx/html;
        index front.html;
        try_files $uri /front.html;
    }

    location /api/ {
        proxy_pass http://backend:3000/;
        proxy_set_header Host $host;
    }
}